  斐波那契数列第n项为前两项的和，设置递归出口后直接调用递归即可
class Solution {
public:
    int Fibonacci(int n) {
    if(n==0)
        return 0;
    if(n==1)
        return 1;
    int arr[40]={0,1};
    return Fibonacci(n-1)+Fibonacci(n-2);
    }
};
