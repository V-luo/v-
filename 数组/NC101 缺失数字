由于是从0-n，所以直接遍历，若值与下标不相等，直接返回下标
class Solution {
public:
    int solve(vector<int>& a) {
        // write code here
        int i;
        for(i = 0;i<a.size();i++)
            if(a[i]!=i)
                return i;
        return i;
    }
};
