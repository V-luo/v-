由于是从0-n，所以直接遍历，若值与下标不相等，直接返回下标
class Solution {
public:
    /**
     * 代码中的类名、方法名、参数名已经指定，请勿修改，直接返回方法规定的值即可
     *
     * 找缺失数字
     * @param a int整型vector 给定的数字串
     * @return int整型
     */
    int solve(vector<int>& a) {
        // write code here
        int i;
        for(i = 0;i<a.size();i++)
            if(a[i]!=i)
                return i;
        return i;
    }
};
